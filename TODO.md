# LabLens AI - Run Project Locally (without Docker)

## Steps
- [x] 1. Analyze task and explore project structure
- [x] 2. Understand config/.env/DB dependencies
- [x] 3. Confirm local run approach (Docker not installed)
- [ ] 4. Add `aiosqlite` to requirements for SQLite async support
- [ ] 5. Create/update `backend/.env` with local SQLite config (non-secret)
- [ ] 6. Create Python venv and install backend dependencies
- [ ] 7. Start backend (uvicorn) and verify /health + /docs
- [ ] 8. Install frontend deps and start Vite dev server
- [ ] 9. Verify frontend loads and /api proxy reaches backend
